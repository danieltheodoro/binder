#!/bin/bash
set -eux

# Kernel Bash
python3 -m bash_kernel.install

# Kernel JavaScript
npm install -g ijavascript
ijsinstall --install=local

# Kernel R
Rscript -e "IRkernel::installspec(user = FALSE)"

# Kernel Julia (já é detectado se instalado via conda-forge)
